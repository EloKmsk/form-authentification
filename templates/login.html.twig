<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulaire PHP</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>

<body>
<div class="container">
<h1>Formulaire d'authentification</h1>
    <form action="" method="post" novalidate>
    <div class="form-group">
        <label for="validationDefaultUsername"><strong>Login</strong></label>
        <input type="text" class="form-control" id="validationDefaultUsername" placeholder="Login" name="login" value="{{formData.login}}" required>
            {% if errors.login is defined %}
                <div class="error text-danger">
                    {{ errors.login }}
                </div>
            {% endif %}
    </div>
    <div class="form-group">
        <label for="exampleInputPassword1" class="form-label"><strong>Mot de passe</strong></label>
        <input class="form-control" type="password" name="password" placeholder="Mot de passe" id="exampleInputPassword1" value="{{formData.password}}" required>
            {% if errors.password is defined %}
                <div class="error text-danger">
                    {{ errors.password }}
                </div>
            {% endif %}
    </div>
    <button type="submit" class="btn btn-primary">Valider</button>
    </form>
</div>
</body>

</html>